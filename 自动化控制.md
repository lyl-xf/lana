# 自动化流程控制开发指南

本文说明在 Lana 网关中实现**自动化流程**（顺序写控制、等待设备反馈、超时/取消）时应调用哪些 API、数据从哪来、推荐怎样分层。与 [`扩展开发指南.md`](扩展开发指南.md) 配套阅读。

---

## 1. 结论（先读这段）

| 动作 | 推荐 API | 说明 |
|------|----------|------|
| **写入 / 触发** | `IDeviceDebugApi.WriteVariableAsync` 或 `WriteAsync` | 走统一 IO 队列、可记历史、写成功会 Patch 缓存与 UI |
| **读反馈（边控边看）** | **优先** `IDeviceDataSnapshotStore` / 后台轮询缓存 | 不额外占 IO，与手动操作页左侧同源 |
| **强制即时读** | `IDeviceDebugApi.ReadVariableAsync` | 仅在没有轮询、或必须同步读 PLC 时用；高频率会挤占 Poll |
| **不要** | 绕过 Scheduler 自己 `Open` 会话 | 会与 Poll / MQTT / 手动操作抢连接，RTU 易冲突 |

**简短回答：写控制可以、也应该用 `DeviceDebugApi`；看状态不要靠循环 `Read`，应订阅轮询后的共享状态。**

---

## 2. 现有数据流（你要接在哪）

```
登录成功
  └─ MainViewModel 创建单例：
       DeviceIoScheduler   ← 所有读写 / Poll 排队
       DevicePointCache    ← Poll 结果（全 IncludeInPoll 点）
       DeviceDataSnapshotStore ← UI 状态（仅 ShowInStatus 点）
       DataCollectionWorker ← 按 PollInterval 后台 Poll
       DeviceDebugApi      ← 对外读写入口
```

```
                    ┌─────────────────────┐
  自动化流程写入 ──►│  IDeviceDebugApi    │──高优先级──┐
  手动操作按钮  ──►│  (DeviceDebugApi)   │            │
  MQTT 下行指令 ──►└─────────────────────┘            ▼
                                            ┌──────────────────┐
                                            │ DeviceIoScheduler │
                                            │ (per 物理链路队列) │
                                            └────────┬─────────┘
                                                     │ Write / Read / Poll
                                                     ▼
                                                  PLC / 设备
                                                     │
                     Poll 完成 ──────────────────────┤
                                                     ▼
                              ┌──────────────────────────────────┐
                              │ DevicePointCache (完整采集缓存)   │
                              └───────────────┬──────────────────┘
                                              │ 过滤 ShowInStatus
                                              ▼
                              ┌──────────────────────────────────┐
                              │ DeviceDataSnapshotStore (UI 快照) │
                              └───────────────┬──────────────────┘
                                              │
                     自动化读反馈 ◄───────────┘ 订阅 ValueText / Version
                     手动操作页左侧 ◄───────────┘
```

---

## 3. 为什么写要用 `IDeviceDebugApi`

`DeviceDebugApi` 是对外的**标准写入入口**，内部调用链为：

```
IDeviceDebugApi.WriteVariableAsync
  → GatewayDeviceService.DebugWriteAsync
    → IDeviceIoScheduler.WriteAsync(..., patchVariable)
      → 高优先级队列执行
      → 成功且变量有别名 → PatchPoint → 更新缓存 + 快照 UI
      → DeviceOperationHistoryService（可选）
```

好处：

1. **与 Poll / MQTT / 手动操作共用 `DeviceIoScheduler`**，同一条 RS485 / 同一 PLC IP 不会并发冲突。
2. **写成功自动 Patch** 到 `DevicePointCache` 和「状态展示」快照，自动化侧立刻能在快照里看到写入后的值（若该点勾选了状态展示）。
3. **`DeviceDebugContext`** 可区分来源、是否记历史，便于审计。

```csharp
await _debugApi.WriteVariableAsync(
    deviceId,
    variableId,
    "1",
    new DeviceDebugContext
    {
        Source = "Automation",      // 自定义流程名，如 "LineStartup"
        RecordHistory = true,       // 量产流程可 true；内部微调可 false
    });
```

### 何时不用 DebugApi、直接碰 Scheduler？

仅当你在做**框架内部扩展**（例如新的 MQTT 指令类型）且需要 Patch 变量时，才在 Gateway 层直接调 `IDeviceIoScheduler`。**业务自动化请始终用 `IDeviceDebugApi`。**

---

## 4. 边看反馈边控制：推荐流程

典型场景：写「启动命令」→ 等「运行状态=1」→ 写「下一步」。

### 4.1 物模型配置（必做）

作为**反馈**的状态点：

- ✅ **参与轮询**
- ✅ **状态展示**（进入 `DeviceDataSnapshotStore`，自动化可读）
- ✅ 填写**别名**（非 HttpClient）
- 设备 **PollInterval > 0**，全局 **启用轮询查询**

作为**控制**的命令点：

- 读写权限为 **Write** 或 **ReadWrite**
- 自动化通过 `WriteVariableAsync` 写入，**不要求**加入手动操作页

### 4.2 控制循环模式（推荐）

```
┌─────────────┐
│  Step 1 写  │
└──────┬──────┘
       ▼
┌─────────────────────┐
│ 等待反馈 (读快照)    │◄─── 订阅 / 轮询 Version，不 Read PLC
│ 条件满足或超时       │
└──────┬──────────────┘
       ▼
┌─────────────┐
│  Step 2 写  │
└─────────────┘
```

**原则：**

- **写**：`IDeviceDebugApi`（每步一次或少量几次）。
- **等**：读 `DeviceDataSnapshotStore` 或 `DevicePointCache.GetVersion`，**不要** `while` 里 `ReadVariableAsync`（会把 IO 队列打满，拖慢 Poll）。

### 4.3 从快照读取当前值

快照里只有勾了「状态展示」的点；按 `deviceId` + `variableId` 查找：

```csharp
static DeviceLivePoint? FindLivePoint(
    IDeviceDataSnapshotStore store,
    long deviceId,
    long variableId)
{
    var group = store.Groups.FirstOrDefault(g => g.DeviceId == deviceId);
    return group?.Points.FirstOrDefault(p => p.VariableId == variableId);
}

static string? GetFeedbackText(
    IDeviceDataSnapshotStore store,
    long deviceId,
    long variableId)
    => FindLivePoint(store, deviceId, variableId)?.ValueText;
```

Bool 反馈可解析：

```csharp
static bool ParseBoolText(string? text)
    => bool.TryParse(text, out var b) && b;
```

### 4.4 等待条件（带超时）

用 **轮询间隔 + 缓存版本** 等待，而不是同步 Read：

```csharp
/// <summary>
/// 等待某反馈点 ValueText 满足条件（依赖后台 Poll 刷新快照）。
/// </summary>
async Task<bool> WaitForFeedbackAsync(
    IDeviceDataSnapshotStore store,
    IDevicePointCache cache,
    long deviceId,
    long variableId,
    Func<string?, bool> predicate,
    TimeSpan timeout,
    CancellationToken cancellationToken)
{
    var deadline = DateTime.UtcNow + timeout;
    long lastVersion = cache.GetVersion(deviceId);

    while (DateTime.UtcNow < deadline)
    {
        cancellationToken.ThrowIfCancellationRequested();

        var text = GetFeedbackText(store, deviceId, variableId);
        if (predicate(text))
            return true;

        // 等设备 Poll 推进缓存（Version 变化说明至少完成一轮采集）
        await Task.Delay(100, cancellationToken);
        var version = cache.GetVersion(deviceId);
        if (version != lastVersion)
            lastVersion = version;
    }

    return false;
}
```

用法示例：

```csharp
// 写启动
var wr = await _debugApi.WriteVariableAsync(devId, startVarId, "true",
    new DeviceDebugContext { Source = "Automation", RecordHistory = true });
if (!wr.Success) { /* 失败处理 */ return; }

// 等运行反馈 == true，最多 30 秒
var ok = await WaitForFeedbackAsync(
    _snapshotStore, _pointCache, devId, runningVarId,
    text => ParseBoolText(text),
    TimeSpan.FromSeconds(30),
    ct);

if (!ok) { /* 超时 */ return; }

// 下一步写...
```

### 4.5 UI 绑定（可选）

若自动化页也要显示当前步、实时反馈：

- 绑定 `IDeviceDataSnapshotStore.Groups`（与手动操作页相同，**勿 Copy 到新集合**）。
- 绑定自建 `AutomationRunState`（当前步骤名、是否运行中、最近错误）。
- 在 UI 线程更新 VM 属性；自动化引擎在后台 `Task` 跑，用 `Dispatcher.UIThread.InvokeAsync` 回写 UI。

---

## 5. 建议的模块划分

在 `Gateway/Services/` 或 `Services/Automation/` 增加流程引擎，**不要**把状态机写在 ViewModel 里。

```
AutomationEngine          ← 跑流程、CancellationToken、步骤表
AutomationStep            ← 单步：Write / Wait / Delay / Branch
IAutomationRunHost        ← Start / Stop / 查询状态
AutomationViewModel       ← 仅绑定状态 + 启停按钮
```

**构造注入（在 MainViewModel 组装）：**

```csharp
var automation = new AutomationEngine(
    _debugApi,
    _snapshotStore,
    _pointCache,
    _deviceService);   // 查变量 Address / DataType
```

| 组件 | 职责 |
|------|------|
| `AutomationEngine` | 解释步骤 JSON / 脚本，顺序执行 |
| `IDeviceDebugApi` | 所有写、必要时单次读 |
| `IDeviceDataSnapshotStore` | 等待反馈、UI 展示 |
| `IDevicePointCache` | `GetVersion` 判断 Poll 是否推进 |
| `GatewayDeviceService` | 加载 Device / Variable 元数据 |

---

## 6. IO 与并发注意事项

1. **同物理链路串行**  
   同一 RS485 口 / 同一 PLC IP 上的写、读、Poll 都在一条队列。自动化多步写会**自然排队**，无需自己加锁。

2. **写会插队 Poll**  
   设计等待超时时，要大于 **设备 PollInterval + 网络/PLC 响应时间**（例如 Poll 1s，等待条件建议 ≥ 3～5s 量级起步）。

3. **多设备并行**  
   不同 `DeviceLinkKey` 的队列相互独立，可用 `Task.WhenAll` 跑多条并行流程；**同一设备**建议单流程串行。

4. **登出**  
   `MainViewModel.OnLogout` 会 `StopAsync` Scheduler、Clear 缓存与快照。自动化须在登出前 `Cancel` 所有运行中流程。

5. **历史数据**  
   高频内部步（如 Pulse 中间态）可 `RecordHistory = false`；关键步（启动、停止、配方切换）保持 `true`。

---

## 7. 可选扩展：从缓存按别名读（headless 场景）

当前 `IDevicePointCache` 对外主要是 `GetVersion` / `GetTelemetryPayload`，**没有**按 alias 读单点。若自动化不想依赖「状态展示」过滤，可在后续增加：

```csharp
// 建议新增到 IDevicePointCache
bool TryGetPoint(long deviceId, string alias, out CachedPointValue point);
```

这样反馈点只需 **参与轮询**，不必勾选状态展示，仍可从缓存读。实现前可临时用快照 + 必开「状态展示」。

---

## 8. 反模式（避免）

| 反模式 | 问题 |
|--------|------|
| `while (!ok) await ReadVariableAsync(...)` | 占满 IO 队列，Poll  starvation，RTU 超时 |
| 直接 `new ModbusTcpClient` 写 | 与 Scheduler 双连接，数据不一致 |
| 复制 `SnapshotStore.Groups` 到新集合再绑定 | 收不到 Worker 更新 |
| 反馈点未参与轮询 | 快照永不更新，等待永远超时 |
| 在 UI 线程跑 `await` 长流程 | 界面卡死；应用 `Task.Run` + 进度回调 |

---

## 9. 最小可运行示例（伪流程）

```csharp
public sealed class SampleStartupFlow
{
    private readonly IDeviceDebugApi _debug;
    private readonly IDeviceDataSnapshotStore _store;
    private readonly IDevicePointCache _cache;

    public SampleStartupFlow(
        IDeviceDebugApi debug,
        IDeviceDataSnapshotStore store,
        IDevicePointCache pointCache)
    {
        _debug = debug;
        _store = store;
        _cache = pointCache;
    }

    public async Task RunAsync(CancellationToken ct)
    {
        const long deviceId = 1;
        const long cmdStartId = 10;
        const long fbRunningId = 11;

        var ctx = new DeviceDebugContext { Source = "SampleStartup", RecordHistory = true };

        var write = await _debug.WriteVariableAsync(deviceId, cmdStartId, "1", ctx);
        if (!write.Success)
            throw new InvalidOperationException(write.Error);

        var running = await WaitForFeedbackAsync(
            _store, _cache, deviceId, fbRunningId,
            t => t == "1" || t?.Equals("true", StringComparison.OrdinalIgnoreCase) == true,
            TimeSpan.FromSeconds(30),
            ct);

        if (!running)
            throw new TimeoutException("设备未在 30s 内进入运行状态");
    }

    // WaitForFeedbackAsync 见 §4.4
}
```

---

## 10. 相关文件索引

| 文件 | 作用 |
|------|------|
| `Gateway/Services/IDeviceDebugApi.cs` | 读写对外契约 |
| `Gateway/Services/DeviceDebugApi.cs` | 实现 + 历史 |
| `Gateway/Services/IDeviceIoScheduler.cs` | IO 仲裁 |
| `Gateway/Services/IDevicePointCache.cs` | Poll 缓存 |
| `Gateway/Services/IDeviceDataSnapshotStore.cs` | UI / 自动化读反馈 |
| `Gateway/Services/DeviceVariablePollRules.cs` | 轮询 / 状态展示规则 |
| `Gateway/Services/DataCollectionWorker.cs` | 后台 Poll |
| `ViewModels/MainViewModel.cs` | 服务组装入口 |

---

## 11. 检查清单（上线前）

- [ ] 反馈变量：参与轮询 + 状态展示 + 别名
- [ ] 控制变量：Write / ReadWrite，地址与类型正确
- [ ] 设备 PollInterval、全局轮询开关已开
- [ ] 写入走 `IDeviceDebugApi`，`Source` 设为流程名
- [ ] 等待逻辑读快照 / Version，非循环 Read
- [ ] 超时、取消、失败分支已实现
- [ ] 登出时取消运行中流程

如需在仓库里落地 `AutomationEngine` 骨架或 `IDevicePointCache.TryGetPoint`，可在需求里说明步骤表格式（JSON / 脚本 / 可视化编排），再按本指南扩展。
