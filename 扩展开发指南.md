# Lana 扩展开发指南

本文配合代码中的 XML 注释，说明如何二次开发自定义功能。项目为 **.NET 9 + Avalonia 12**，**无 DI 容器**，服务在 `MainViewModel` 中手工组装。

## 启动与组装

```
Program → App（建库/主题）→ MainViewModel（服务图）
  ├─ 未登录：LoginViewModel
  └─ 已登录：DataCollectionWorker + ShellViewModel（各页面）
```

- 数据库路径：应用目录下 `lana.db`（见 `DbPath`，相对 `AppContext.BaseDirectory`）
- View 绑定：`ViewLocator` 将 `XxxViewModel` 反射为 `XxxView`
- 设备读写（推荐）：`IDeviceDebugApi`（自动记历史）

## 常见扩展清单

### 1. 新增侧栏页面

1. 新建 `ViewModels/XxxViewModel.cs`、`Views/XxxView.axaml(.cs)`（命名遵循 ViewLocator）
2. 在 `MainViewModel` 构造需要的服务，经 `ShellViewModel` 构造函数传入
3. 在 `ShellViewModel` 的 `_pages` 注册，并增加 Navigate 命令 / 标题 / `IsXxxSelected`
4. 在 `ShellView.axaml` 增加侧栏按钮；Admin 专属参考 Devices/Cameras

### 2. 新增协议

1. `ProtocolType` 增加枚举（连续编号）
2. 实现 `IDeviceProtocolSession`，或扩展 `IoTClientFactory`
3. `ProtocolSessionFactory.CreateSession` 分支
4. `ProtocolDisplay` / 设备管理 UI / 校验上限
5. 若采集 JSON 与 Alias 读取不同，改 `DataCollectionWorker`

### 3. 新增设备/物模型字段

1. `Entities.cs` 属性
2. `GatewaySchema.EnsureColumnAsync` 迁移（已有库靠 ALTER，勿只改 CREATE）
3. Mapper SQL + `GatewayDeviceService` 规范化
4. `DevicesView` / ViewModel 绑定
5. 若需备份：更新 `GatewayBackupDtos` 映射

### 4. 自定义「定义页面」按钮行为

配置入口：设备管理 → 物模型 →「进入自定义页」等字段。

运行时逻辑：`DefinedPageViewModel.CreateButton` / `ExecuteButtonAsync` / Bool 点动（`DefinedPageView.axaml.cs`）。

- Bool/Coil/Discrete：按下 `true`、松开 `false`
- 非 Bool + Write：写 `DefinedPageWriteValue`
- 非 Bool + Read：读地址

扩展新 Kind：改枚举 → CreateButton → 执行逻辑 → AXAML 模板。

### 5. 摄像头能力

`Camera` → `CameraSchema` → `CameraMapper` → `CameraService` → UI。  
播放：`BuildPlayRequest` + `LibVlcHost`。定义页播放全部 `IsEnabled` 摄像头。

### 6. 后台任务

参考 `DataCollectionWorker`：用 `IGatewayConfigStore` 读配置，在登录 Start、登出 Stop。

### 7. 新设置项

`SettingKeys` → `ISettingsService` → `SettingsViewModel` / UI。

### 8. 新持久化模块

`XxxSchema.EnsureAsync` → 在 `DbInitializer` 调用 → Mapper → Service → `MainViewModel` 组装。

## 目录速查

| 目录 | 职责 |
|------|------|
| `ViewModels/` / `Views/` | UI 与页面逻辑 |
| `Gateway/` | 设备、协议、采集、MQTT、调试、历史 |
| `Cameras/` | 摄像头配置与 LibVLC |
| `Data/` | SQLite、用户、设置 |
| `Services/` | 认证、设置 |
| `Themes/` | Aurora / Snow |

## 默认账号

| 用户 | 密码 | 角色 |
|------|------|------|
| admin | 123456 | Admin |
| demo | demo | Member |

更细的类型/方法说明见各文件 XML 注释（IDE 悬停即可查看）。
